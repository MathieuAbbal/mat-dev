<header
    class="sticky top-0 z-50 bg-white/80 dark:bg-slate-950/80 backdrop-blur-xl border-b border-gray-200/50 dark:border-slate-800/50 transition-all duration-300"
    id="main-header">
    <nav class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <!-- Logo -->
            <a href="#" class="text-xl font-bold relative group">
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 to-cyan-500 dark:from-[#818cf8] dark:to-[#22d3ee]">MA</span>
                <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gradient-to-r from-indigo-600 to-cyan-500 group-hover:w-full transition-all duration-300"></span>
            </a>

            <!-- Desktop Navigation -->
            <div class="hidden md:flex items-center space-x-1">
                <a href="#about" data-section="about"
                    class="nav-link px-4 py-2 text-gray-600 dark:text-slate-300 hover:text-indigo-600 dark:hover:text-indigo-400 font-medium transition-colors relative">
                    Profil
                    <span class="nav-indicator absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-indigo-600 dark:bg-indigo-400 rounded-full transition-all duration-300"></span>
                </a>
                <a href="#skills" data-section="skills"
                    class="nav-link px-4 py-2 text-gray-600 dark:text-slate-300 hover:text-indigo-600 dark:hover:text-indigo-400 font-medium transition-colors relative">
                    Compétences
                    <span class="nav-indicator absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-indigo-600 dark:bg-indigo-400 rounded-full transition-all duration-300"></span>
                </a>
                <a href="#experience" data-section="experience"
                    class="nav-link px-4 py-2 text-gray-600 dark:text-slate-300 hover:text-indigo-600 dark:hover:text-indigo-400 font-medium transition-colors relative">
                    Expérience
                    <span class="nav-indicator absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-indigo-600 dark:bg-indigo-400 rounded-full transition-all duration-300"></span>
                </a>
                <a href="#projects" data-section="projects"
                    class="nav-link px-4 py-2 text-gray-600 dark:text-slate-300 hover:text-indigo-600 dark:hover:text-indigo-400 font-medium transition-colors relative">
                    Projets
                    <span class="nav-indicator absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-indigo-600 dark:bg-indigo-400 rounded-full transition-all duration-300"></span>
                </a>
                <a href="#contact" data-section="contact"
                    class="nav-link px-4 py-2 text-gray-600 dark:text-slate-300 hover:text-indigo-600 dark:hover:text-indigo-400 font-medium transition-colors relative">
                    Contact
                    <span class="nav-indicator absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-indigo-600 dark:bg-indigo-400 rounded-full transition-all duration-300"></span>
                </a>

                <!-- Dark mode toggle -->
                <div class="flex items-center ml-4 pl-4 border-l border-gray-200/50 dark:border-slate-800/50">
                    <button id="theme-toggle" class="relative w-14 h-7 rounded-full bg-gray-200 dark:bg-slate-700 transition-colors duration-300 focus:outline-none">
                        <div class="theme-toggle-dot absolute top-0.5 left-0.5 w-6 h-6 rounded-full bg-white shadow-sm flex items-center justify-center transition-transform duration-300">
                            <svg class="sun-icon w-4 h-4 text-amber-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
                            </svg>
                            <svg class="moon-icon w-4 h-4 text-indigo-500 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
                            </svg>
                        </div>
                    </button>
                </div>
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden flex items-center gap-3">
                <button id="theme-toggle-mobile" class="p-2 rounded-xl text-gray-600 dark:text-slate-300 hover:bg-gray-100 dark:hover:bg-slate-800 transition-colors">
                    <svg class="sun-icon-m w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
                    </svg>
                    <svg class="moon-icon-m w-5 h-5 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
                    </svg>
                </button>
                <button id="navbar-toggle"
                    class="p-2 rounded-xl text-gray-600 dark:text-slate-300 hover:bg-gray-100 dark:hover:bg-slate-800 transition-colors">
                    <svg class="hamburger-icon w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                    <svg class="close-icon w-6 h-6 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
        </div>

        <!-- Mobile Navigation -->
        <div id="navbar-collapse" class="hidden md:hidden pb-4 overflow-hidden transition-all duration-300">
            <div class="flex flex-col space-y-1 pt-2">
                <a href="#about"
                    class="px-4 py-3 text-gray-600 dark:text-slate-300 hover:bg-indigo-50 dark:hover:bg-indigo-500/10 hover:text-indigo-600 dark:hover:text-indigo-400 rounded-xl font-medium transition-all">Profil</a>
                <a href="#skills"
                    class="px-4 py-3 text-gray-600 dark:text-slate-300 hover:bg-indigo-50 dark:hover:bg-indigo-500/10 hover:text-indigo-600 dark:hover:text-indigo-400 rounded-xl font-medium transition-all">Compétences</a>
                <a href="#experience"
                    class="px-4 py-3 text-gray-600 dark:text-slate-300 hover:bg-indigo-50 dark:hover:bg-indigo-500/10 hover:text-indigo-600 dark:hover:text-indigo-400 rounded-xl font-medium transition-all">Expérience</a>
                <a href="#projects"
                    class="px-4 py-3 text-gray-600 dark:text-slate-300 hover:bg-indigo-50 dark:hover:bg-indigo-500/10 hover:text-indigo-600 dark:hover:text-indigo-400 rounded-xl font-medium transition-all">Projets</a>
                <a href="#contact"
                    class="px-4 py-3 text-gray-600 dark:text-slate-300 hover:bg-indigo-50 dark:hover:bg-indigo-500/10 hover:text-indigo-600 dark:hover:text-indigo-400 rounded-xl font-medium transition-all">Contact</a>
            </div>
        </div>
    </nav>
</header>

<script>
    // Mobile menu toggle
    const navbarToggle = document.getElementById('navbar-toggle');
    const navbarCollapse = document.getElementById('navbar-collapse');
    const hamburgerIcon = document.querySelector('.hamburger-icon');
    const closeIcon = document.querySelector('.close-icon');

    navbarToggle?.addEventListener('click', () => {
        navbarCollapse?.classList.toggle('hidden');
        hamburgerIcon?.classList.toggle('hidden');
        closeIcon?.classList.toggle('hidden');
    });

    // Dark mode toggle
    const html = document.documentElement;
    const themeToggle = document.getElementById('theme-toggle');
    const themeToggleMobile = document.getElementById('theme-toggle-mobile');

    function updateIcons(isDark: boolean) {
        const sunIcons = document.querySelectorAll('.sun-icon, .sun-icon-m');
        const moonIcons = document.querySelectorAll('.moon-icon, .moon-icon-m');
        const dot = document.querySelector('.theme-toggle-dot') as HTMLElement;

        sunIcons.forEach(el => el.classList.toggle('hidden', isDark));
        moonIcons.forEach(el => el.classList.toggle('hidden', !isDark));
        if (dot) dot.style.transform = isDark ? 'translateX(28px)' : 'translateX(0)';
    }

    const isDark = localStorage.getItem('darkMode') === 'true' ||
        (!localStorage.getItem('darkMode') && window.matchMedia('(prefers-color-scheme: dark)').matches);
    if (isDark) html.classList.add('dark');
    updateIcons(isDark);

    function toggleTheme() {
        const nowDark = html.classList.toggle('dark');
        localStorage.setItem('darkMode', String(nowDark));
        updateIcons(nowDark);
    }

    themeToggle?.addEventListener('click', toggleTheme);
    themeToggleMobile?.addEventListener('click', toggleTheme);

    // Active section indicator
    const sections = document.querySelectorAll('section[id]');
    const navLinks = document.querySelectorAll('.nav-link');

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                const id = entry.target.id;
                navLinks.forEach(link => {
                    const indicator = link.querySelector('.nav-indicator') as HTMLElement;
                    if (link.getAttribute('data-section') === id) {
                        link.classList.add('text-indigo-600', 'dark:text-indigo-400');
                        if (indicator) indicator.style.width = '60%';
                    } else {
                        link.classList.remove('text-indigo-600', 'dark:text-indigo-400');
                        if (indicator) indicator.style.width = '0';
                    }
                });
            }
        });
    }, { rootMargin: '-20% 0px -70% 0px' });

    sections.forEach(section => observer.observe(section));

    // Header shadow on scroll
    const header = document.getElementById('main-header');
    window.addEventListener('scroll', () => {
        if (window.scrollY > 10) {
            header?.classList.add('shadow-sm');
        } else {
            header?.classList.remove('shadow-sm');
        }
    });

    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const href = (e.currentTarget as HTMLAnchorElement).getAttribute('href');
            if (href) {
                const target = document.querySelector(href);
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    navbarCollapse?.classList.add('hidden');
                    hamburgerIcon?.classList.remove('hidden');
                    closeIcon?.classList.add('hidden');
                }
            }
        });
    });
</script>
